name: Customer Mobile OTA

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  update:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/customer-mobile

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: apps/customer-mobile/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Install EAS CLI
        run: npm install -g eas-cli

      - name: Ensure EAS project is initialized
        run: eas project:info --non-interactive || eas project:init --non-interactive --force
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}

      - name: EAS Update (main branch)
        run: eas update --branch main --non-interactive --message "auto update"
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
